<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.daitdayoung.user">
	<!-- 제조국에 대한 제조사 조회 -->
	<select id="myCoursesList" resultType="kr.co.daitdayoung.user.domain.UserMyCoursesDomain" parameterType="String">
	select  c.cou_Code, c.cou_Name, i.ins_Name, c.banner_Img, crg.crg_Code, ui.ui_Name,
			crg.progress_Rate, crg.registrationdate, crg.completiondate
	from 	COURSES c, INSTRUCTOR i, COURSE_REGISTRATION crg, USER_INFO ui
	where 	ui.ui_id = #{ui_id}
	</select>
	
	
	
	
</mapper>