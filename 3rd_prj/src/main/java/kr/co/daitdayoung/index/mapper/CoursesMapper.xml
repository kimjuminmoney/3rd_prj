<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.daitdayoung.index">

	<!-- 문의글이 적힌 게시판의 모든 내용 조회 -->
	<select id="" resultType="" parameterType="String">
	
	SELECT COU_CODE, INS_ID, COU_NAME, CONTENT, TO_CHAR(OPENDATE, 'YYYY-MM-DD')AS OPENDATE,
				BANNER_IMG, COU_PERIOD, SAMPLE_VIDEO, APPROVE, REFUSE_REANSON, HITS, CC_CODE, ADMIN_ID, 
				SC_CODE, MC_CODE
	FROM   COURSES
	ORDER BY COU_CODE ASC;
	
	
	</select>
	
</mapper>